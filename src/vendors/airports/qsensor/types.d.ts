export type QSensor_AirportSearchResult = {
  city: string;
  iata: string;
  link: string;
  name: string;
};

export type QSensor_TerminalMetadata = {
  estimatedWaitMinutes: number;
  name: string;
};
